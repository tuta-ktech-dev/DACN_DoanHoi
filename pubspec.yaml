name: doan_hoi_app
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

  # Core dependencies
  cupertino_icons: ^1.0.2
  get_it: ^7.6.4
  connectivity_plus: ^5.0.1
  shared_preferences: ^2.2.2
  dio: ^5.3.3
  pretty_dio_logger: 1.4.0
  dartz: ^0.10.1

  # State management
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5

  # UI dependencies
  flutter_svg: ^2.0.9
  cached_network_image: ^3.3.0
  shimmer: ^3.0.0

  # Firebase
  # (Removed Firebase deps; will add later when integrating FCM)

  # Utilities
  intl: ^0.20.2
  url_launcher: ^6.2.2
  image_picker: ^1.0.5
  mobile_scanner: ^3.5.5

  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
    - assets/fonts/
